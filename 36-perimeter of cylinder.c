#include <stdio.h>
int main() {
    int radius,height,perimeter;
    printf("enter the radius:");
    scanf("%d",&radius);
    printf("enter the height:");
    scanf("%d",&height);
    printf("%d",perimeter=4*radius+2*height);
    return 0;
}
